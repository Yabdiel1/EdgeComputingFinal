To Run Serial FFT:
1. Go to src/tests/ and run python test_serial.py

To run Parallel FFT:
1. Go to root directory
2. Run this command in terminal: `chmod +x run_parallel.sh`
3. Run this command in terminal: `./run_parallel.sh`
